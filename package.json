{
  "name": "baddalni-react-root",
  "version": "1.0.0",
  "description": "Root project manager for Laravel Backend and React Frontend",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"cd backend && php artisan serve\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
